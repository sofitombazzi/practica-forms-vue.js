// Creo mi instancia principal
const app = Vue.createApp({
    data(){
        return{
            // Acá va mi contenido estático
        }
    }
});

// Aca van mis componentes, templates, methods


// Acá monto la instancia 
app.mount('.contenedor');